# Example env file for local development
# Copy to .env.local and fill real values. DO NOT commit .env.local.

# Public Supabase client (used in browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-only keys (do NOT expose to client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
KAKAO_REST_API_KEY=your-kakao-rest-api-key

# Optional: public Kakao key (if used client-side; prefer server proxy)
NEXT_PUBLIC_KAKAO_REST_API_KEY=your-kakao-public-key

# Debug endpoints (development only) - set to true to enable dev-only endpoints
# WARNING: never enable in production
# ENABLE_DEBUG_ENDPOINTS=true

# Notes:
# - Create .env.local from this and add .env.local to .gitignore
# - For Vercel/GitHub Actions, add server-only keys in project secrets
# - Use Supabase CLI to link local project if desired
# Example environment variables for local development
# Copy this file to `.env.local` and fill in real values.

# Public (safe to expose) Supabase client values
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Server-only keys (DO NOT prefix with NEXT_PUBLIC_)
# The Kakao REST API key must remain server-side and be referenced by
# `process.env.KAKAO_REST_API_KEY` in `pages/api/kakao-address.ts`.
KAKAO_REST_API_KEY=your_kakao_rest_api_key

# (Optional) Supabase service role key - ONLY for admin server endpoints
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
# Supabase 설정
# https://supabase.com 에서 프로젝트를 생성하고 아래 값을 입력하세요

NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# 예시:
# NEXT_PUBLIC_SUPABASE_URL=https://abcdefghijk.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
